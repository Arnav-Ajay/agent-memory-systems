[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-memory-systems"
version = "0.1.0"
description = "Agent pipeline with planner, tools, and memory"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["agent_memory_systems"]
